# Domain Checker Bot

Telegram бот для проверки срока действия доменов через WHOIS.

## Возможности

- Проверка срока действия доменов через WHOIS
- Уведомление если домен истекает < 31 дней
- **Автопроверка раз в день** (09:00) с уведомлениями
- Удобный **кнопочный интерфейс** (без команд)
- Управление списком доменов (добавление/удаление/редактирование)
- Хранение доменов в локальном JSON файле

## Установка

```bash
# Переходим в директорию
cd domain-checker-bot

# Создаём виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или: venv\Scripts\activate  # Windows

# Устанавливаем зависимости
pip install -r requirements.txt
```

## Настройка

1. Получите токен бота у [@BotFather](https://t.me/BotFather)
2. Установите токен:

```bash
export TELEGRAM_BOT_TOKEN='ваш_токен'
```

Или отредактируйте `config.py`:
```python
BOT_TOKEN = "ваш_токен"
```

## Запуск

```bash
python bot.py
```

Для работы в фоне (Linux):
```bash
nohup python bot.py > bot.log 2>&1 &
```

Или через systemd (создайте `/etc/systemd/system/domain-bot.service`):
```ini
[Unit]
Description=Domain Checker Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/domain-checker-bot
ExecStart=/path/to/domain-checker-bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## Интерфейс бота

После `/start` появляется постоянная клавиатура:

```
┌─────────────────┬──────────────────┐
│ 🔍 Проверить все │ ⚠️ Истекающие    │
├─────────────────┼──────────────────┤
│ 📋 Список       │ 🔎 Проверить один │
├─────────────────┼──────────────────┤
│ ➕ Добавить     │ ➖ Удалить        │
├─────────────────┼──────────────────┤
│ ✏️ Изменить     │ ❓ Помощь         │
└─────────────────┴──────────────────┘
```

| Кнопка | Описание |
|--------|----------|
| 🔍 Проверить все | Проверить все домены из списка |
| ⚠️ Истекающие | Показать только домены < 31 день |
| 📋 Список | Показать все сохранённые домены |
| 🔎 Проверить один | Проверить любой домен (не из списка) |
| ➕ Добавить | Добавить домен в список |
| ➖ Удалить | Удалить домен из списка |
| ✏️ Изменить | Переименовать домен |
| ❓ Помощь | Справка |

## Автопроверка

Бот автоматически проверяет домены **каждый день в 09:00**.

Если есть домены, истекающие менее чем через 31 день, бот отправляет уведомление всем подписанным пользователям:

```
🔔 Ежедневная проверка доменов

⚠️ Истекают в ближайшие 31 дней:

🔴 example.com
   Истекает: 01.02.2026
   Статус: ИСТЕКАЕТ ЧЕРЕЗ 18 дней!
   Регистратор: REG.RU
```

**Подписка автоматическая** — каждый, кто взаимодействует с ботом, получает уведомления.

## Индикаторы статуса

| Индикатор | Значение |
|-----------|----------|
| 🟢 | более 60 дней до истечения |
| 🟡 | 31-60 дней |
| 🔴 | менее 31 дня (требует внимания!) |

## Структура проекта

```
domain-checker-bot/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация (токен, настройки)
├── domain_manager.py   # CRUD операции с доменами
├── whois_checker.py    # WHOIS проверка
├── domains.json        # Список доменов
├── users.json          # Подписчики на уведомления
├── requirements.txt    # Зависимости
├── test_bot.py         # Тестовый скрипт
└── README.md
```

## Конфигурация

Файл `config.py`:

```python
BOT_TOKEN = "ваш_токен"      # Токен от @BotFather
DOMAINS_FILE = "domains.json" # Файл с доменами
EXPIRY_WARNING_DAYS = 31      # Порог предупреждения (дни)
```

## Примеры файлов

### domains.json
```json
{
  "domains": [
    "google.com",
    "github.com",
    "example.com"
  ]
}
```

### users.json
```json
{
  "users": [123456789, 987654321]
}
```

## Тестирование

Запуск тестов без Telegram:
```bash
python test_bot.py
```

## Зависимости

- `python-telegram-bot[job-queue]` — Telegram Bot API
- `python-whois` — WHOIS запросы
- `aiohttp` — асинхронные HTTP запросы
